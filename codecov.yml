codecov:
  require_ci_to_pass: true
  notify:
    wait_for_ci: true

ignore:
  - "tests/*"

coverage:
  status:
    project:
      default:
        target: 90%
    patch:
      default:
        target: 90%
